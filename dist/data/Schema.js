// Generated by CoffeeScript 2.2.2
(function() {
  var Checker, Schema, get;

  get = require('./get');

  Checker = require('./Schema.Checker');

  module.exports = Schema = class Schema {
    constructor() {
      this.check = this.check.bind(this);
      this.rule = this.rule.bind(this);
      this.format = this.format.bind(this);
      this._rules = {};
      this._formats = {};
      this.format('email', string.isEmail);
      this.format('url', string.isURL);
    }

    check(data, path) {
      //#######################################
      //|
      //|  @params {object} opt
      //|
      //#######################################
      if (path) {
        data = get(data, path);
      }
      return new Checker({
        rules: this._rules,
        formats: this._formats,
        data: data
      });
    }

    rule(name, check) {
      //#######################################
      //|
      //|  @params {string}   name
      //|  @params {function} check(data)
      //|
      //|  @return {Schema} this
      //|
      //#######################################
      this._rules[name] = check;
      return this;
    }

    format(name, check) {
      //#######################################
      //|
      //|  @params {string}   name
      //|  @params {function} check(data)
      //|
      //|  @return {Schema} this
      //|
      //#######################################
      this._formats[name] = check;
      return this;
    }

  };

}).call(this);
